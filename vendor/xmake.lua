target("ImGui")
    set_group("ext")
    set_default(false)
    add_includedirs(".")
    set_kind("static")
    add_headerfiles("ImGui/**.h")
    add_files("ImGui/**.cpp")
    add_syslinks("user32", "gdi32", "shell32", "d3d11", "dxgi")
target_end()

target("Math")
    set_group("ext")
    set_default(false)
    add_includedirs(".")
    set_kind("static")
    add_headerfiles("Math/**.h")
    add_files("Math/**.cpp")
target_end()

target("cgltf")
    set_group("ext")
    set_default(false)
    add_includedirs(".")
    set_kind("static")
    add_headerfiles("cgltf/**.h")
    add_files("cgltf/**.c")
target_end()

